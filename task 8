( SELECT CITY, LENGTH(CITY) AS CITY_LENGTH FROM STATION ORDER BY LENGTH(CITY), CITY LIMIT 1 ) UNION ALL ( SELECT CITY, LENGTH(CITY) AS CITY_LENGTH FROM STATION ORDER BY LENGTH(CITY) DESC, CITY LIMIT 1 );
